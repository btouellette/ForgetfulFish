{
  "name": "@forgetful-fish/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "prisma generate",
    "lint": "eslint .",
    "typecheck": "cross-env DATABASE_URL=postgresql://postgres:postgres@localhost:5432/forgetful_fish prisma validate",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.4.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prisma": "^6.4.1"
  }
}
